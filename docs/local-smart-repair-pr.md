# Pull request notes

The source-built local repair button was disabled because the development launcher intentionally used an unconfigured channel. This change adds a loopback-only signed development channel without changing production trust.
